{
  "v": "v349",
  "deployed": "2026-08-06T00:00:00Z",
  "change": "Fix 'Page Unresponsive' freeze on load: the immediate version-check on page load used a SYNCHRONOUS XHR that blocked the entire page until the network request completed, which could take many seconds on a slow connection and trigger the browser's own hang-detector. Converted to async with identical reload-if-outdated behavior, so a slow connection can never freeze the page."
}
