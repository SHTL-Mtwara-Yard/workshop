{
  "v": "v364",
  "deployed": "2026-08-06T00:00:00Z",
  "change": "Manual Exit date range fix: instead of a fixed 3-day-back cutoff (which kept blocking legitimate cases like a truck sitting a week+ due to internet issues), the earliest selectable exit date is now the truck's own Gate-In date -- an exit can never be earlier than when the truck actually arrived, which is the real constraint, not an arbitrary day count. Max is still capped at today."
}
